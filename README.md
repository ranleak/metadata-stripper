# metadata-stripper
Remove non-essential metadata from any image, video, or document file.

This script requires ```exiftool```. Please install it with ```sudo apt-get install exiftool```.

For your convenience you can run this script via Docker. See [the instructions](Docker_Usage.md) to find information on how.